import React from 'react';
import classes from './Spinner.css';

const spinner = () => (
    <div style={{marginLeft: '50%', marginTop: '12%'}} className={classes.loader}></div>
)

export default spinner;
